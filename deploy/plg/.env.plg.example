# deploy/plg/.env.plg.example 用于单独启动 Prometheus、Loki、Grafana 时覆盖端口和抓取目标。
# 如果你通过 make monitor-up 启动整套监控，很多值会被脚本自动合并；这里只保留最常见的手工覆盖项。

# ====================
# 常用修改
# ====================

# SUMMARIX_METRICS_TARGET：用途：Prometheus 抓取 Summarix 后端指标时使用的目标地址；可选值：host:port 形式，例如 summarix-backend:8000 或 host.docker.internal:8000；默认值：Compose 默认 summarix-backend:8000。
SUMMARIX_METRICS_TARGET=summarix-backend:8000

# PROMETHEUS_PORT：用途：Prometheus 暴露到宿主机的端口；可选值：1 到 65535 的整数；默认值：Compose 默认 9090。
PROMETHEUS_PORT=9090

# GRAFANA_PORT：用途：Grafana 暴露到宿主机的端口；可选值：1 到 65535 的整数；默认值：Compose 默认 3000。
GRAFANA_PORT=3000

# GRAFANA_ADMIN_USER：用途：Grafana 管理员用户名；可选值：任意非空字符串；默认值：Compose 默认 admin。
GRAFANA_ADMIN_USER=admin

# GRAFANA_ADMIN_PASSWORD：用途：Grafana 管理员密码；可选值：任意非空字符串；默认值：Compose 默认 please-change-me，生产或共享环境建议改掉。
GRAFANA_ADMIN_PASSWORD=please-change-me

# LANGWATCH_PUBLIC_URL：用途：Grafana Dashboard 中跳转到 LangWatch 时使用的公开地址；可选值：合法 http/https URL；默认值：Compose 默认 http://localhost:5560。
LANGWATCH_PUBLIC_URL=http://localhost:5560

# ====================
# 不常用修改
# ====================

# LOKI_PORT：用途：Loki 暴露到宿主机的端口；可选值：1 到 65535 的整数；默认值：Compose 默认 3100。
LOKI_PORT=3100
