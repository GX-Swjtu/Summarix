# .env.api.key 只放你手动维护的第三方 Key；监控脚本会自动补齐其它运行时环境变量和本地 secret。
# 建议把真实 Key 与根目录 .env 分开维护，避免把密钥和普通配置混在一起。

# ====================
# 常用修改
# ====================

# DASHSCOPE_API_KEY：用途：为 Summarix 默认的 Qwen 模型提供鉴权；可选值：阿里云 DashScope 签发的 API Key；默认值：空，必须手动填写后才能直接使用默认真实模型。
DASHSCOPE_API_KEY=

# LANGWATCH_API_KEY：用途：当 Summarix 需要把 trace 上报到 LangWatch 项目时使用；可选值：LangWatch 项目后台签发的 sk-lw-...；默认值：空。首次执行 make monitor-up 时，如果这里为空，脚本会在 LangWatch 页面启动后按官方流程提示你生成并粘贴该 Key。
LANGWATCH_API_KEY=

# OPENAI_API_KEY：用途：给 LangWatch 或你手动切换的 LiteLLM 模型提供 OpenAI 能力；可选值：OpenAI 平台签发的 API Key；默认值：空。
OPENAI_API_KEY=

# ANTHROPIC_API_KEY：用途：给 LangWatch 或你手动切换的 LiteLLM 模型提供 Claude 能力；可选值：Anthropic 平台签发的 API Key；默认值：空。
ANTHROPIC_API_KEY=

# AZURE_OPENAI_API_KEY：用途：给 LangWatch 或你手动切换的模型提供 Azure OpenAI 能力；可选值：Azure OpenAI 资源的 API Key；默认值：空。
AZURE_OPENAI_API_KEY=

# GROQ_API_KEY：用途：给 LangWatch 或你手动切换的模型提供 Groq 能力；可选值：Groq 平台签发的 API Key；默认值：空。
GROQ_API_KEY=

# GOOGLE_API_KEY：用途：给直接走 Google AI Studio / Gemini API 的场景提供鉴权；可选值：Google AI 平台签发的 API Key；默认值：空。
GOOGLE_API_KEY=

# ====================
# 不常用修改
# ====================

# DASHSCOPE_API_BASE：用途：覆盖 DashScope OpenAI 兼容接口地址；可选值：合法 HTTP/HTTPS URL；默认值：项目默认使用 https://dashscope.aliyuncs.com/compatible-mode/v1，通常不需要改。
# DASHSCOPE_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# GOOGLE_APPLICATION_CREDENTIALS：用途：为 Vertex AI 或其它 Google Cloud 能力提供服务账号凭据；可选值：本地凭据文件路径，或部署环境约定的凭据注入值；默认值：空。
GOOGLE_APPLICATION_CREDENTIALS=

# VERTEXAI_PROJECT：用途：指定 Vertex AI 所属 GCP 项目；可选值：有效的 GCP Project ID；默认值：空。
VERTEXAI_PROJECT=

# VERTEXAI_LOCATION：用途：指定 Vertex AI 区域；可选值：如 global、us-central1、asia-east1 等 Vertex AI 支持的区域；默认值：空。
VERTEXAI_LOCATION=